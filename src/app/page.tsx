import ToolBar from "@/components/Toolbar";
import DynamicSheetTable from "@/components/Table";

const HomePage = () => {
  return (
    <main className="min-h-screen">
      <ToolBar />
      <DynamicSheetTable />
    </main>
  );
};

export default HomePage;
